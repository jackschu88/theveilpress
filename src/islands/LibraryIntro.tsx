/**
 * Old cinematic curtain open — Library only, once per session.
 * Site chrome stays the new press shell; this is just the first-open.
 */
import VeilIntro from '../components/cinematic/VeilIntro';

export default function LibraryIntro() {
  return <VeilIntro />;
}
