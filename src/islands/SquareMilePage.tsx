/**
 * Exact production book page (SquareMile view) under the new press chrome.
 */
import ProductShell from './ProductShell';
import SquareMile from '../views/SquareMile';

export default function SquareMilePage() {
  return (
    <ProductShell>
      <SquareMile />
    </ProductShell>
  );
}
